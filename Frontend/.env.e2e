# E2E-mode (подхватывается `vite --mode e2e`, FE-TASK-055 / §10.3).
# Включает MSW-worker в браузере → все /api/* идут через tests/msw/handlers/*.
# Не коммитить credentials: реальные секреты через .env.e2e.local (см. .gitignore).
VITE_ENABLE_MSW=true
